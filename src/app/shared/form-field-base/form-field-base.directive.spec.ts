import { FormFieldBaseDirective } from './form-field-base.directive';

describe('FormFieldBaseDirective', () => {
  it('should create an instance', () => {
    const directive = new FormFieldBaseDirective();
    expect(directive).toBeTruthy();
  });
});
